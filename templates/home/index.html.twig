{% extends 'base.html.twig' %}

{% block title %}BideoGamu{% endblock %}

{% block stylesheets %}
	<link rel="stylesheet" href="/css/index.css">
{% endblock %}

{% block body %}

<div class="videogames">
        {% for videogame in videogames %}
            <div class="encart">
                <div class="full">
                    <a href="{{ path("videogames_index") }}"><img src={{ videogame.img }}></a>
                </div>
                    <div class ="cliquable">
                        <h2>{{ videogame.name }}</h2>
                        <p>Catégorie : {{ videogame.category.name }}<p>
                        <p>{{ videogame.description| striptags | slice(0, 200) }} <a href="">[...]</a></p>
                    </div>
            </div>
        {% else %}
            <p>No game fund.</p>
        {% endfor %}
    </div>

{% endblock %}
