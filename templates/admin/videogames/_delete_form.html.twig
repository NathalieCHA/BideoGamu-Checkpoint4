<form method="post" action="{{ path('adminvideogames_delete', {'id': videogame.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ videogame.id) }}">
    <button class="btn"><i class="far fa-trash-alt"></i></button>
</form>
