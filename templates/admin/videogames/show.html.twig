{% extends 'front/base.html.twig' %}

{% block title %}<title>videogame</title>{% endblock %}

{% block body %}
    <h1>videogame</h1>
    {% for videogame in videogames %}
                {{ videogame.name }}
                <img src={{ videogame.img }}>

    <a href="{{ path('adminvideogames_edit', {'id': videogame.id}) }}">edit</a>

    {{ include('admin/videogames/_delete_form.html.twig') }}
        {% endfor %}
    <a href="{{ path('admin') }}">back to admin home</a>

{% endblock %}
