{{ form_start(form) }}
    <div class="">{{ form_label(form.name) }}</div>
        <div> {{ form_widget(form.name) }} </div>

        <small>{{ form_help(form.name) }}</small>

        <div class="form-error">
            {{ form_errors(form.name) }}
    </div>

    <button class="btn">{{ button_label|default('Add this new catogory') }}</button>
{{ form_end(form) }}
