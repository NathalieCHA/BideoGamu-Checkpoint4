{{ form_start(form) }}

<div class="forms">
    <div class="">{{ form_label(form.name) }}</div>
        <div> {{ form_widget(form.name) }} </div>

        <small>{{ form_help(form.name) }}</small>

        <div class="form-error">
            {{ form_errors(form.name) }}
        </div>
    <button>{{ button_label|default('add this new catogory') }}</button>
</div>
{{ form_end(form) }}
